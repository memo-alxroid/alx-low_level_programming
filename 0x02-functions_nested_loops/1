#include <stdio.h>

/**
 * _islower - Entry Point
 * @c: a charcahter to check
 *
 * Description: checks for lowercase character
 *
 *
 * Return: 1 if c is lowercase 0 otherwise1 if c is lowercase 0 otherwise1 if c is lowercase 0 otherwise1 if c is lowercase 0 otherwise1 if c is lowercase 0 otherwise1 if c is lowercase 0 otherwise1 if c is lowercase 0 otherwise1 if c is lowercase 0 otherwise1 if c is lowercase 0 otherwise1 if c is lowercase 0 otherwise1 if c is lowercase 0 otherwise	}
}

